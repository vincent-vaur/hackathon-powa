<form method="post" action="{{ path('post_it_delete', {'id': post_it.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_it.id) }}">
    <button class="btn">Delete</button>
</form>
